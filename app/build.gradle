// Add this line to enable the ndk plugin
apply plugin: 'com.android.library'

// Define the external native build configuration
externalNativeBuild {
    cmake {
        // Set the path to your CMakeLists.txt file
        path "CMakeLists.txt"
    }
}

// ...

